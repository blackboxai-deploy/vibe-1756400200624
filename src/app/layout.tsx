import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'RadiologyAI - AI-Powered X-Ray Analysis',
  description: 'Professional radiology platform with AI-powered diagnostic report generation for X-ray images.',
  keywords: ['radiology', 'AI', 'medical imaging', 'X-ray analysis', 'diagnostic reports'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
          {children}
        </div>
      </body>
    </html>
  )
}